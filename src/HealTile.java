import java.awt.image.BufferedImage;

public class HealTile extends SolidSprite {

    public HealTile(BufferedImage image, double x, double y, double width, double height) {
        super(image, x, y, width, height);
    }
}
